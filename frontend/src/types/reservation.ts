export interface Reservation { id: number; confirmation_code: string; property_name?: string; channel: string; status: string; check_in_date: string; check_out_date: string; nights: number; guests_count: number; guest_name: string; guest_email: string; nightly_rate: number; cleaning_fee: number; service_fee: number; taxes: number; total_amount: number; discount_amount: number; points_earned: number; points_redeemed: number; guest_notes: string; confirmed_at: string | null; cancelled_at: string | null; checked_in_at: string | null; checked_out_at: string | null; created_at: string }
export interface AvailabilityResult { available: boolean; property_id: number; check_in: string; check_out: string; nights: number; reason: string | null }
export interface CalendarDay { date: string; available: boolean; price: number; min_stay: number }
export interface PriceCalculation { property_id: number; nights: number; nightly_rate: number; subtotal: number; cleaning_fee: number; service_fee: number; taxes: number; gross_total: number; tier_discount: { tier_name: string; percent: number; amount: number } | null; loyalty: { points_available: number; max_redeemable: number; max_discount: number } | null; total_before_points: number; min_total: number; currency: string; points_to_redeem?: number; points_discount?: number; charge_amount?: number; total_discount?: number }
export interface BookingResult { reservation_id: number; confirmation_code: string; stripe_client_secret: string; charge_amount: number; breakdown: PriceCalculation }
