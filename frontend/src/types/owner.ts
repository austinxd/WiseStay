export interface DashboardSummary { properties_count: number; active_properties: number; total_reservations_ytd: number; upcoming_reservations: number; active_guests_now: number; revenue: { current_month: number; previous_month: number; ytd: number; month_over_month_change: number }; occupancy: { current_month_percent: number; previous_month_percent: number; ytd_percent: number }; payouts: { last_payout_amount: number; last_payout_date: string | null; pending_amount: number; next_payout_date: string }; channel_breakdown_ytd: Record<string, { count: number; revenue: number }>; alerts: Alert[] }
export interface Alert { type: string; message: string; device_id?: number; alert_id?: number }
export interface PropertyPerformance { property: { id: number; name: string; city: string; status: string; cover_image_url: string | null }; revenue: { total: number; commission: number; net_to_owner: number; average_nightly_rate: number; by_month: { month: string; revenue: number; reservations: number }[] }; occupancy: { total_percent: number; by_month: { month: string; percent: number; nights_booked: number }[] }; reservations: { total: number; by_channel: Record<string, number>; by_status: Record<string, number>; average_stay_nights: number; average_guests: number }; devices: { id: number; display_name: string; device_type: string; brand: string; status: string; battery_level: number | null }[]; recent_noise_alerts: { id: number; decibel_level: number; severity: string; created_at: string; resolved_at: string | null }[] }
export interface RevenueReport { period: string; properties: PropertyRevenue[]; totals: { gross_revenue: number; total_commission: number; total_net: number; total_reservations: number } }
export interface PropertyRevenue { property_id: number; property_name: string; gross_revenue: number; commission_rate: number; commission_amount: number; net_revenue: number; reservations_count: number; by_channel: Record<string, number> }
export interface OccupancyDay { date: string; status: 'booked' | 'available' | 'blocked'; guest_name?: string; confirmation_code?: string; channel?: string; reason?: string }
export interface OwnerProfile { email: string; first_name: string; last_name: string; company_name: string; commission_rate: number; stripe_connected: boolean; is_payout_enabled: boolean; payout_day: number }
